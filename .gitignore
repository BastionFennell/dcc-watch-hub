node_modules
dist
.DS_Store
*.log
.env*
coverage

# Author reference material (copyrighted; not part of the site)
/Carl Character Sheet*.pdf
*.pdf

# Author source material: filled character sheets and full-res renders live outside git; optimized copies ship under public/img
/character-sheets/
/character-portraits/

# 012: the compiled crawler dossier. Source lives in content/status/*.json;
# `npm run build:dossier` (prebuild, predev) regenerates this on every build.
public/data/dossier/
