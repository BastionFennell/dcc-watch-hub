# Quickstart: NPC encounters + System Registry
- `http://localhost:5180/ep/1?fake=1&t=<after the first npc event>` → Encountered strip under the party rail; tap a chip → entity record; scrub back → chips/facts vanish.
- Phone (400×800): NPCs tab.
- `http://localhost:5180/registry` → sections by episode, search, kind chips; `/registry#hoarder` opens that entry.
## Manual acceptance
SC-601 sweep at npc boundaries; SC-602 registry sections/facts/appearances; SC-603 hash + "Open in the Registry"; SC-604 gates + Lighthouse a11y 100 on both pages.
